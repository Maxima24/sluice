# Frontend public env (inlined at build time by Next). Copy to .env.local.
# Points directly at the backend (the backend's CORS_ORIGINS must allow this origin
# for client-side calls; SSR fetches are same-process and unaffected).
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_WS_URL=ws://localhost:3000
