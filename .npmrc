# pnpm settings for the workspace.
# React 19 / Next 16 / NestJS ecosystems have noisy peer ranges — don't fail install on them.
strict-peer-dependencies=false
# If a tool trips on pnpm's isolated node_modules (phantom deps), uncomment:
# node-linker=hoisted
